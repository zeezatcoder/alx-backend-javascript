/**
 * Display messages on the STDOUT.
 * @param {String} msg message to display.
 * @author Azeezat Maja <https://github.com/zeezatcoder>
 */
const displayMessage = (msg) => {
  console.log(msg);
};

module.exports = displayMessage;
